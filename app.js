 //  const data = JSON.stringify({message: 'Hello, World!' });
    // //  res.send(data); })
//Listing for the request with parameters of a port variable and a function
//Console logging which port the rever is listening to for other developers